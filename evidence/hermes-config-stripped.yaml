name: hermes-orchestrator
description: "The Brain - Persistent memory orchestrator for Forge 2"
version: 1.0.0

providers:
  default: gemini
  models:
    - name: gemini-3.1-flash-lite
      provider: gemini
      api_key: "REDACTED_GEMINI_KEY" # Removed for security

skills_directory: "./skills"
cron:
  enabled: true
  directory: "~/.hermes/cron"

channels:
  main: "#sprint-main"
  coder: "#agent-coder"
  log: "#agent-log"

slack:
  bot_token: "xoxb-REDACTED" # Removed for security
  app_token: "xapp-REDACTED" # Removed for security
